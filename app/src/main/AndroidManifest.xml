<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.graduation">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- 读写内存块权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- 调用相机权限 -->
    <uses-permission android:name="android.permission.CAMERA" /> <!-- 读取SD卡的权限:打开相册选取图片所必须的权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ParentMyclassActivity"></activity>
        <activity android:name=".MyCLassStudentActivity" />
        <activity android:name=".AddHomeWorkActivity" />
        <activity android:name=".SelectClassActivity" />
        <activity android:name=".CheckHistoryActivity" />
        <activity android:name=".ErrorBookActivity" />
        <activity android:name=".PracticesActivity" />
        <activity android:name=".MyClassActivity" />
        <activity android:name=".PhoneResultsActivity" />
        <activity android:name=".SearchSchoolActivity" />
        <activity android:name=".UserIdentityActivity" />
        <activity android:name=".UserNameActivity" />
        <activity android:name=".UserInfoActivity" />
        <activity android:name=".ParentActivity" />
        <activity android:name=".TeacherActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ImportActivity" />
        <activity android:name=".LogonActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>